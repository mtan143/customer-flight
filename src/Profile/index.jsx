import React from 'react';
import PropTypes from 'prop-types';

Profile.propTypes = {
    
};

function Profile(props) {
    return (
        <div>
            <h1>Đạt đẹp trai</h1>
        </div>
    );
}

export default Profile;